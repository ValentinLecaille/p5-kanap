// page d'affichage du panier

/*const products = // fetch tous les produits, comme fait dans la page d'accueil -> Array qui contient tous les détails de tous les produits

const panier = // récupérer la liste d'articles du local storage -> Array

for(let i = 0; i < panier; i++)
{
	// pour chaque article du panier, on son id
	// on utilise cet id pour trouver les information du produit correspondant dans la liste de tous les produits

	const product = products.find((item) => item.id == panier.idProduct);
	// "item" représente un élément du Array "products"
	// "item.id == panier.idProduct" représente la condition pour trouver le produit qu'on veut.
	
	// maintenant, "product" contient toutes les information de l'article du panier, on peut les afficher dans la page.
}*/